specVersion: "v1"
serviceId: "Daybreak"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "daybreak-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
